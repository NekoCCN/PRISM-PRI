from setuptools import setup, find_packages

setup(
    name="prism",
    version="0.1.0",
    packages=find_packages(),
    author="Culith",
    description="PRISM project for wind turbine defect detection.",
    python_requires='>=3.8',
)